spring:
  application:
      name: infsus
  docker:
      compose:
        enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5433/infsus
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
